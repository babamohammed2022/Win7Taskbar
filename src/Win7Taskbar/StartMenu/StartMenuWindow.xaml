<Window x:Class="Win7Taskbar.StartMenu.StartMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Win7Taskbar.StartMenu"
        Title="Win7Taskbar Start Menu"
        Width="416" Height="548"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Topmost="True"
        ShowActivated="True"
        WindowStartupLocation="Manual"
        Left="-32000" Top="-32000"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        FontFamily="Segoe UI"
        FontSize="12"
        SourceInitialized="OnSourceInitialized"
        Deactivated="OnDeactivated"
        PreviewKeyDown="OnPreviewKeyDown"
        PreviewTextInput="OnPreviewTextInput">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InvBoolToVis"/>
        <local:NullToVisibilityConverter x:Key="NullToVis"/>

        <!-- Win7 left-pane row: pale cyan hover, 1px glass border. -->
        <Style x:Key="LeftRowStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="1,0,1,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3" Padding="2,1" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Stretch"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Padding" Value="0"/>
                            </DataTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsEnabled" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#A07BA7CE"/>
                                <Setter TargetName="Bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#E8F4FCFF" Offset="0"/>
                                            <GradientStop Color="#C0D4F0FC" Offset="0.5"/>
                                            <GradientStop Color="#A8C4E8F8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </MultiTrigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#B0709CC8"/>
                                <Setter TargetName="Bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#D0E8F8FF" Offset="0"/>
                                            <GradientStop Color="#A0C0E4F4" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <EventSetter Event="MouseLeftButtonUp" Handler="OnLeftItemClick"/>
            <EventSetter Event="MouseRightButtonUp" Handler="OnLeftItemContext"/>
        </Style>

        <!-- Win7 right pane: white text, brighter hover on the glass. -->
        <Style x:Key="RightRowStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="2,0,6,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" BorderBrush="Transparent"
                                BorderThickness="1" CornerRadius="3" Padding="7,2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Padding" Value="0"/>
                            </DataTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsEnabled" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#C0FFFFFF"/>
                                <Setter TargetName="Bd" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#90FFFFFF" Offset="0"/>
                                            <GradientStop Color="#50C8E4F8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <EventSetter Event="MouseLeftButtonUp" Handler="OnRightItemClick"/>
            <EventSetter Event="MouseRightButtonUp" Handler="OnRightItemContext"/>
            <EventSetter Event="MouseEnter" Handler="OnRightItemMouseEnter"/>
        </Style>

        <!-- Drawn Aero scrollbar (no Microsoft bitmaps). Used by All Programs. -->
        <Style x:Key="AeroScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="17"/>
            <Setter Property="MinWidth" Value="17"/>
            <Setter Property="Background" Value="#FFE8E8E8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="#FFE4E4E4">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="17"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="17"/>
                            </Grid.RowDefinitions>
                            <RepeatButton Grid.Row="0" Command="ScrollBar.LineUpCommand"
                                          Focusable="False">
                                <RepeatButton.Template>
                                    <ControlTemplate TargetType="RepeatButton">
                                        <Border Background="#FFF0F0F0" BorderBrush="#FFA0A0A0" BorderThickness="1">
                                            <Path Data="M0,4 L4,0 L8,4 Z" Fill="#FF404040"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </RepeatButton.Template>
                            </RepeatButton>
                            <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="#FFE8E8E8"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border x:Name="ThumbBd" Margin="1,0" CornerRadius="2"
                                                        BorderBrush="#FF8AA0B8" BorderThickness="1">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#FFF8FBFF" Offset="0"/>
                                                            <GradientStop Color="#FFD0DCE8" Offset="0.45"/>
                                                            <GradientStop Color="#FFB8C8D8" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="ThumbBd" Property="BorderBrush" Value="#FF5A9FD4"/>
                                                        <Setter TargetName="ThumbBd" Property="Background">
                                                            <Setter.Value>
                                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                                    <GradientStop Color="#FFE8F4FC" Offset="0"/>
                                                                    <GradientStop Color="#FF7EC0F0" Offset="0.5"/>
                                                                    <GradientStop Color="#FF4A9AD8" Offset="1"/>
                                                                </LinearGradientBrush>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Focusable="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="#FFE8E8E8"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                            <RepeatButton Grid.Row="2" Command="ScrollBar.LineDownCommand"
                                          Focusable="False">
                                <RepeatButton.Template>
                                    <ControlTemplate TargetType="RepeatButton">
                                        <Border Background="#FFF0F0F0" BorderBrush="#FFA0A0A0" BorderThickness="1">
                                            <Path Data="M0,0 L8,0 L4,4 Z" Fill="#FF404040"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </RepeatButton.Template>
                            </RepeatButton>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer Aero frame: drawn gradients (no Microsoft bitmaps). -->
    <Border Margin="10,10,10,2" CornerRadius="8,8,0,0" BorderThickness="1"
            BorderBrush="#C06A8BB0">
        <Border.Effect>
            <DropShadowEffect Color="#A0000000" BlurRadius="18" ShadowDepth="0" Opacity="0.65"/>
        </Border.Effect>
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#55D0E8F8" Offset="0"/>
                <GradientStop Color="#4088B0C8" Offset="0.45"/>
                <GradientStop Color="#307098B0" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border CornerRadius="7,7,0,0" BorderThickness="1" BorderBrush="#90FFFFFF" Margin="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="242"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Left programs pane: opaque white (no glass). -->
                <Border Grid.Column="0" Margin="7,10,5,8" CornerRadius="3"
                        BorderBrush="#90A0B4C8" BorderThickness="1"
                        Background="#FFFFFFFF">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="32"/>
                        </Grid.RowDefinitions>

                        <ListBox x:Name="LeftList"
                                 ItemsSource="{Binding LeftItems}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="2,4,2,2"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 ItemContainerStyle="{StaticResource LeftRowStyle}"
                                 KeyDown="OnLeftKeyDown"
                                 MouseRightButtonUp="OnLeftListContext"
                                 Visibility="{Binding IsSearching, Converter={StaticResource InvBoolToVis}}">
                            <ListBox.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource AeroScrollBar}"/>
                            </ListBox.Resources>
                            <ListBox.Style>
                                <Style TargetType="ListBox">
                                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AllProgramsOpen}" Value="True">
                                            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ListBox.Style>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="37" Background="Transparent">
                                        <Rectangle Height="1" Fill="#C0C8D4" VerticalAlignment="Center" Margin="8,0">
                                            <Rectangle.Style>
                                                <Style TargetType="Rectangle">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Rectangle.Style>
                                        </Rectangle>
                                        <Grid Margin="{Binding IndentMargin}">
                                            <Grid.Style>
                                                <Style TargetType="Grid">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Grid.Style>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="12"/>
                                                <ColumnDefinition Width="36"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="16"/>
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="0" Fill="#3A5068"
                                                  VerticalAlignment="Center" HorizontalAlignment="Center"
                                                  Stretch="Uniform" Width="6" Height="8">
                                                <Path.Style>
                                                    <Style TargetType="Path">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Setter Property="Data" Value="M0,0 L6,4 L0,8 Z"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                <Setter Property="Data" Value="M0,0 L8,0 L4,6 Z"/>
                                                                <Setter Property="Width" Value="8"/>
                                                                <Setter Property="Height" Value="6"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Path.Style>
                                            </Path>
                                            <Image Grid.Column="1" Source="{Binding Icon}" Width="32" Height="32"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Name}"
                                                       VerticalAlignment="Center" Margin="4,0,4,0"
                                                       FontSize="12" Foreground="#1A1A1A"
                                                       TextTrimming="CharacterEllipsis"/>
                                            <Path Grid.Column="3" Data="M0,0 L5,4 L0,8 Z" Fill="#3A5068"
                                                  VerticalAlignment="Center" HorizontalAlignment="Center"
                                                  Visibility="{Binding JumpVisibility}"/>
                                        </Grid>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <ListBox x:Name="SearchList"
                                 ItemsSource="{Binding SearchHits}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="2,4"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                 ItemContainerStyle="{StaticResource LeftRowStyle}"
                                 Visibility="{Binding IsSearching, Converter={StaticResource BoolToVis}}">
                            <ListBox.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource AeroScrollBar}"/>
                            </ListBox.Resources>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Height="28">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="28"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Image Source="{Binding Icon}" Width="16" Height="16"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                   VerticalAlignment="Center" Margin="4,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <!-- Programs, a hairline, then "> All Programs" as a row. -->
                        <Grid Grid.Row="1"
                              Visibility="{Binding IsSearching, Converter={StaticResource InvBoolToVis}}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Rectangle Height="1" Fill="#C0C8D4" Margin="8,4,8,2"/>
                            <Border x:Name="AllProgramsRow" Grid.Row="1"
                                    Background="Transparent" BorderBrush="Transparent"
                                    BorderThickness="1" CornerRadius="3"
                                    Padding="4,3" Margin="1,0,1,2" Cursor="Hand"
                                    MouseLeftButtonUp="OnAllProgramsFooter"
                                    MouseRightButtonUp="OnAllProgramsContext">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="#A07BA7CE"/>
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                            <GradientStop Color="#E8F4FCFF" Offset="0"/>
                                                            <GradientStop Color="#C0D4F0FC" Offset="0.5"/>
                                                            <GradientStop Color="#A8C4E8F8" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M0,0 L6,4 L0,8 Z" Fill="#3A5068" Width="6" Height="8"
                                          Stretch="Uniform" Margin="8,0,8,0"
                                          VerticalAlignment="Center"
                                          Visibility="{Binding AllProgramsOpen, Converter={StaticResource InvBoolToVis}}"/>
                                    <Path Data="M6,0 L0,4 L6,8 Z" Fill="#3A5068" Width="6" Height="8"
                                          Stretch="Uniform" Margin="8,0,8,0"
                                          VerticalAlignment="Center"
                                          Visibility="{Binding AllProgramsOpen, Converter={StaticResource BoolToVis}}"/>
                                    <TextBlock Text="All Programs" FontSize="12" Foreground="#1A1A1A"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding AllProgramsOpen, Converter={StaticResource InvBoolToVis}}"/>
                                    <TextBlock Text="Back" FontSize="12" Foreground="#1A1A1A"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding AllProgramsOpen, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Search lives under the left pane, as on Windows 7. -->
                        <Border Grid.Row="2" Margin="6,2,6,6" Background="#C0FFFFFF"
                                BorderBrush="#90A0B4C8" BorderThickness="1" CornerRadius="2">
                            <Grid>
                                <TextBlock Text="{Binding SearchHint}" Foreground="#808080"
                                           Margin="8,0,26,0" VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="{Binding IsSearching, Converter={StaticResource InvBoolToVis}}"/>
                                <TextBox x:Name="SearchBox"
                                         BorderThickness="0" Background="Transparent"
                                         VerticalContentAlignment="Center"
                                         Padding="6,0,26,0"
                                         FontSize="12"
                                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                                <Grid HorizontalAlignment="Right" Width="22" Margin="0,0,4,0"
                                      IsHitTestVisible="False">
                                    <Ellipse Width="9" Height="9" Stroke="#1A73C7" StrokeThickness="1.7"
                                             HorizontalAlignment="Left" VerticalAlignment="Center"
                                             Margin="2,0,0,2"/>
                                    <Path Data="M10,12 L14.5,16.5" Stroke="#1A73C7" StrokeThickness="2"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Right pane: darker glass, hierarchy, shutdown. -->
                <Border Grid.Column="1" Margin="0,8,8,8" CornerRadius="2">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#283A5878" Offset="0"/>
                            <GradientStop Color="#18283858" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>

                        <Grid Width="48" Height="48" Margin="8,12,8,8"
                              HorizontalAlignment="Center" Cursor="Hand"
                              MouseLeftButtonUp="OnUserPictureClick">
                            <Image x:Name="IconOld" Stretch="UniformToFill"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            <Image x:Name="IconNew" Stretch="Uniform" Opacity="0"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            <Border x:Name="PhotoFrame" CornerRadius="2"
                                    BorderThickness="2" IsHitTestVisible="False">
                                <Border.BorderBrush>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#F0FFFFFF" Offset="0"/>
                                        <GradientStop Color="#A0C8D8E8" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.BorderBrush>
                            </Border>
                        </Grid>

                        <ListBox x:Name="RightList" Grid.Row="1"
                                 ItemsSource="{Binding RightLinks}"
                                 BorderThickness="0" Background="Transparent"
                                 Padding="4,4,0,0"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 ItemContainerStyle="{StaticResource RightRowStyle}"
                                 MouseLeave="OnRightListMouseLeave">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid MinHeight="20">
                                        <Rectangle Height="1" Fill="#60FFFFFF" VerticalAlignment="Center" Margin="4,0">
                                            <Rectangle.Style>
                                                <Style TargetType="Rectangle">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Rectangle.Style>
                                        </Rectangle>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"
                                                   FontSize="12" Foreground="White">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="FontWeight" Value="Normal"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPrimary}" Value="True">
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsSeparator}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel Grid.Row="2" Orientation="Horizontal"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Margin="0,0,5,4">
                            <Border x:Name="ShutdownChrome" CornerRadius="3,0,0,3"
                                    BorderThickness="1,1,0,1" BorderBrush="#90FFFFFF"
                                    Cursor="Hand" MouseLeftButtonUp="OnShutdown">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#90D0E4F4" Offset="0"/>
                                        <GradientStop Color="#7088A8C0" Offset="0.49"/>
                                        <GradientStop Color="#60607090" Offset="0.5"/>
                                        <GradientStop Color="#50405068" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <TextBlock x:Name="ShutdownLabel" Text="Shut down"
                                           Foreground="White" FontSize="12"
                                           Margin="12,4,10,4" VerticalAlignment="Center"/>
                            </Border>
                            <Border x:Name="ShutdownArrow" CornerRadius="0,3,3,0"
                                    BorderThickness="1" BorderBrush="#90FFFFFF"
                                    Width="22" Cursor="Hand"
                                    MouseLeftButtonUp="OnShutdownArrow">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#90D0E4F4" Offset="0"/>
                                        <GradientStop Color="#7088A8C0" Offset="0.49"/>
                                        <GradientStop Color="#60607090" Offset="0.5"/>
                                        <GradientStop Color="#50405068" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Path Data="M0,0 L8,0 L4,5 Z" Fill="White"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Margin="0,2,0,0"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Border>
</Window>
