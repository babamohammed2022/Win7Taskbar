# Cartella di import

Metti qui (drag & drop dal browser GitHub, oppure con `git push`) i bundle
scaricati da MediaFire:

- `win7taskbarforgithub.zip` — codice sorgente del progetto
- `Win7Taskbar-v2.58-win-x64.zip` — build distribuita di riferimento (opzionale)

Al termine dell'import questa cartella viene riorganizzata/svuotata.
